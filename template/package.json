{
  "name": "{{name}}",
  "version": "1.0.0",
  "description": "{{name}}",
  "main": "index.js",
  "scripts": {
    "dev": "pipflow dev",
    "build": "pipflow build",
    "preview": "pipflow task preview"
  },
  "keywords": [],
  "author": "{{author}}",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    {{#if imagemin}}
    "gulp-imagemin": "^7.1.0",
    {{/if}}
    "pipflow": "latest"
  }
}
