{
  "name": "{{name}}",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "pipflow serve --mode=development",
    "build": "cross-env NODE_ENV=production pipflow build --mode=production",
    "preview": "pipflow preview -p 8527",
    "lint": "pipflow lint"
  },
  "keywords": [],
  "author": "{{author}}",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "~7.0.3",
    "pipflow": "latest"
  }
}
